#define($CR)${writer.write(13)}#end
#macro(GET $dato)#if($dato)${dato}#end#end
#foreach( $checada in $checadas )
#GET(${checada.sucursal})|#GET(${checada.fecha})|#GET(${checada.hora})|#GET(${checada.empresa})|#GET(${checada.empleado})|
#end
